apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cephfs-busybox-pvc
  namespace: odf-demo
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: ocs-storagecluster-cephfs
