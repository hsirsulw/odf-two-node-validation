apiVersion: v1
kind: Service
metadata:
  name: timescaledb
  namespace: odf-demo
spec:
  clusterIP: None
  selector:
    app: timescaledb
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
